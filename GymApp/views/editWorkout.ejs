<!DOCTYPE html>
<html lang="en">
<head>
    <title>GymApp - Edit Workout</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-5">
        <h1 class="text-center">Edit Workout</h1>
        <form action="/workouts/edit/<%= workout._id %>" method="POST" class="mt-4">
            <div class="mb-3">
                <label class="form-label">Name:</label>
                <input type="text" name="name" value="<%= workout.name %>" class="form-control" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Type:</label>
                <input type="text" name="type" value="<%= workout.type %>" class="form-control" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Sets:</label>
                <input type="number" name="sets" value="<%= workout.sets %>" class="form-control" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Reps:</label>
                <input type="number" name="reps" value="<%= workout.reps %>" class="form-control" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Rest Time (seconds):</label>
                <input type="number" name="restTime" value="<%= workout.restTime %>" class="form-control" required>
            </div>
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-success">
                    Update Workout <i class="fas fa-check"></i>
                </button>
                <a href="/workouts" class="btn btn-secondary">
                    Cancel <i class="fas fa-times"></i>
                </a>
            </div>
        </form>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
